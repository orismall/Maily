#include "ConsoleOutputHandler.h"
using namespace std;

// Prints the provided message to the standard console output
void ConsoleOutputHandler::print(const string& message) {
    cout << message << endl;
}
